import ComponentDocsHeader from '@/components/website/component-docs-header';
import { SETUP_CODE } from '@/package/registry/setup-code.tsx';
import { CodeBlock } from '@/components/website/code-block.tsx';
import { ComponentPreview } from '@/components/website/component-preview.tsx';
import {
  ToastProvider,
  Toast,
  ToastTitle,
  ToastDescription,
  ToastAction,
} from '@/components/ui/toast.tsx';
import {
  ToastExampleDefault,
  ToastExampleTopRight,
  ToastExampleTopLeft,
  ToastExampleBottomLeft,
  ToastExampleBottomRight,
} from '@/app/components/common/examples/toast-examples.tsx';
import { ComponentManualSetup } from '@/components/sections/component-manual-setup.tsx';
import DocsPageOutline from '@/components/website/docs-page-outline.tsx';

<DocsPageOutline />

<ComponentDocsHeader />

<ComponentManualSetup>
  <CodeBlock hasViewMore fileName={SETUP_CODE['toast'].registerAt}>
    {SETUP_CODE['toast'].code}
  </CodeBlock>
</ComponentManualSetup>

<div class="py-12" />

<ComponentPreview
  code={`export const ToastExampleDefault = () => {
  const [open, setOpen] = React.useState(false);
  const timerRef = React.useRef(0);
  return <ToastProvider>
    <Button
      onClick={() => {
        setOpen(false);
        window.clearTimeout(timerRef.current);
        timerRef.current = window.setTimeout(() => {
          setOpen(true);
        }, 100);
      }}>
      Show toast
    </Button>
    <Toast open={open} onOpenChange={setOpen}>
      <ToastTitle>Testing toast</ToastTitle>
      <ToastDescription>
        Description text for the testing toast
      </ToastDescription>
      <ToastAction altText="Got it">Got it</ToastAction>
    </Toast>
  </ToastProvider>
}`}
  headline="Toast default example">
  <ToastExampleDefault />
</ComponentPreview>

<div class="py-12" />

<ComponentPreview
  code={`<ToastProvider position="top-right">
    <Button
      onClick={() => {
        setOpen(false);
        window.clearTimeout(timerRef.current);
        timerRef.current = window.setTimeout(() => {
          setOpen(true);
        }, 100);
      }}>
      Show toast
    </Button>
    <Toast open={open} onOpenChange={setOpen}>
      <ToastTitle>Top-right toast</ToastTitle>
      <ToastDescription>
        Using position="top-right"
      </ToastDescription>
      <ToastAction altText="Got it">Got it</ToastAction>
    </Toast>
  </ToastProvider>`}
  headline="Toast with position top-right">
  <ToastExampleTopRight />
</ComponentPreview>

<div class="py-12" />

<ComponentPreview
  code={`<ToastProvider position="top-left">
    <Button
      onClick={() => {
        setOpen(false);
        window.clearTimeout(timerRef.current);
        timerRef.current = window.setTimeout(() => {
          setOpen(true);
        }, 100);
      }}>
      Show toast
    </Button>
    <Toast open={open} onOpenChange={setOpen}>
      <ToastTitle>Top-left toast</ToastTitle>
      <ToastDescription>
        Using position="top-left"
      </ToastDescription>
      <ToastAction altText="Got it">Got it</ToastAction>
    </Toast>
  </ToastProvider>`}
  headline="Toast with position top-left">
  <ToastExampleTopLeft />
</ComponentPreview>

<div class="py-12" />

<ComponentPreview
  code={`<ToastProvider position="bottom-left">
    <Button
      onClick={() => {
        setOpen(false);
        window.clearTimeout(timerRef.current);
        timerRef.current = window.setTimeout(() => {
          setOpen(true);
        }, 100);
      }}>
      Show toast
    </Button>
    <Toast open={open} onOpenChange={setOpen}>
      <ToastTitle>Bottom-left toast</ToastTitle>
      <ToastDescription>
        Using position="bottom-left"
      </ToastDescription>
      <ToastAction altText="Got it">Got it</ToastAction>
    </Toast>
  </ToastProvider>`}
  headline="Toast with position bottom-left">
  <ToastExampleBottomLeft />
</ComponentPreview>

<div class="py-12" />

<ComponentPreview
  code={`<ToastProvider position="bottom-right">
    <Button
      onClick={() => {
        setOpen(false);
        window.clearTimeout(timerRef.current);
        timerRef.current = window.setTimeout(() => {
          setOpen(true);
        }, 100);
      }}>
      Show toast
    </Button>
    <Toast open={open} onOpenChange={setOpen}>
      <ToastTitle>Bottom-right toast</ToastTitle>
      <ToastDescription>
        Using position="bottom-right"
      </ToastDescription>
      <ToastAction altText="Got it">Got it</ToastAction>
    </Toast>
  </ToastProvider>`}
  headline="Toast with position bottom-right">
  <ToastExampleBottomRight />
</ComponentPreview>
